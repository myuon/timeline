{
  "private": true,
  "workspaces": [
    "web",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^7.5.0"
  },
  "scripts": {
    "dev": "concurrently \"yarn workspace web dev\" \"yarn workspace server dev\"",
    "prebuild": "rm -rf dist",
    "build": "yarn workspace web build && yarn workspace server build",
    "start:prod": "NODE_ENV=production node dist/server/index.js",
    "deploy": "docker build . --tag asia-northeast1-docker.pkg.dev/default-364617/timeline/latest && docker push asia-northeast1-docker.pkg.dev/default-364617/timeline/latest && gcloud run deploy timeline --image asia-northeast1-docker.pkg.dev/default-364617/timeline/latest --region asia-northeast1"
  }
}
